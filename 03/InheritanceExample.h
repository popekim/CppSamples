#pragma once

namespace samples
{
	// TODO: Please ask me before start this. I'm fixing slides this part now.
	// 1. Complete Animal class and Cat class in the lecture 3 slides (Look at "Inheritance" part in the slide)
		// 1.1. In each constructor and destructor, print some text like bleow
			// In Animal class
				// "Animal - Constructor"
				// "Animal - Destructor"
			// In Cat class
				// "Cat - Constructor"
				// "Cat - Destructor"
		// 1.2. Create int GetAge() const in Animal Class
		// 1.3. Create char* GetName() const in Cat Class
	// 2. In InheritanceExample(), create one Cat* type variable
		// 2.1. Then, print it's age, and name
		// 2.2. Finally, delete the Cat*
	void InheritanceExample();
}