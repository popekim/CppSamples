#pragma once

namespace samples
{
	// TODO
	// 1. Create a class, vector3 (Using Vector2)
		// 1.1. Create float Multiply(const Vector& other)
			// Dot product (Vector * Vector) 
		// 1.2. Create Vector Multiply(const Vector& other)
			// Scalar multiplication (Vector * Scalar)
		// 1.3 Create Print()
			// Printing mX and mY
	// 2. In FunctionOverloadExample(), call two multiply() functions
		// 2.1. And, print the results
	void FunctionOverloadExample();
}